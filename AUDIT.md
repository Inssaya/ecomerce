# مراجعة هندسية — تدقيق تقرير الجلسة الماضية + ما لم يُكتشف

> هذه المراجعة تُكمّل `RAPPORT.md` ولا تُلغيه. قرأتُ الأربع جلسات التي كُتب فيها الكود
> (`2711fc6 → a807612`)، وتحققتُ من **كل** بند من بنود التقرير السابق ضد الكود سطراً بسطر،
> ثم بحثتُ عمّا لم يبحث عنه.
>
> **لم يُغيَّر أي سطر كود.** ما جرى هنا: توحيد الفروع + هذا التدقيق.

---

## ١. الفروع — الواقع الفعلي (يختلف عمّا في `RAPPORT.md`)

التقرير السابق قال إنه أنشأ `main`. هذا صحيح، لكن الصورة الكاملة لم تكن أمامه:

| الفرع | الـ SHA | ما هو فعلاً |
|---|---|---|
| `main` | `a807612` | ✅ المرجع. الجلسات الأربع كاملة |
| `claude/monolith-rebuild-5xzsfl` | `a807612` | نسخة مطابقة لـ `main` حرفياً — **مكرّر** |
| `claude/codebase-line-by-line-review-9szy8p` | `25b1308` | `main` + `RAPPORT.md` فقط |
| `claude/ecommerce-saas-microservices-5xzsfl` | `aee68e2` | **الكود الميت** — معماريّة microservices القديمة |

**نقطة مهمة:** `aee68e2` ليس سلفاً لـ `main`. الفرعان **منفصلان تماماً** — لا يلتقيان في أي commit
(`main` بدأ من `3751675` ثم تباعدا). فرق بينهما: ٢١٩ ملف، ‎+٣٩٨٤٠/−٦٨٤١ سطر.
هذا يعني أن الجلسة التي كتبت `aee68e2` كانت تبني عالماً آخر بالكامل.

### الحل

```
✅ فعلتُه: دمجتُ RAPPORT.md في فرع العمل الحالي (cherry-pick)
           → فرع المراجعة لم يعد له سبب للبقاء
```

ما تفعله أنت في GitHub:

1. **Settings → Branches → Default branch → `main`** (هذه أهم خطوة، وهي سبب كل ما جرى)
2. احذف الثلاثة: `monolith-rebuild-5xzsfl` (مكرّر)، `codebase-line-by-line-review-9szy8p` (دُمج)،
   `ecommerce-saas-microservices-5xzsfl` (كود ميت — لكن **صوّره بـ tag قبل الحذف** إن أردت أثراً:
   `git tag archive/microservices aee68e2 && git push origin archive/microservices`)
3. قاعدة دائمة: كل جلسة تتفرّع من `main` حصراً

---

## ٢. تدقيق البنود الـ٢٤ — ما صحّ وما لم يصحّ

**١٩ من ٢٤ صحيحة ومؤكدة.** ما يلي هو ما يحتاج تصحيحاً أو تحذيراً:

### ٢.١ بند خاطئ في تفاصيله — رقم ١١

التقرير قال: «`delivery_days` يظهر للزبون — أزله من مخططات الاستجابة العامة».

**تحققت: `delivery_days` ليس في المخطط العام أصلاً.** هو موجود فقط في
`ProductWrite` / `ProductPatch` / `ProductAdmin` — أي لوحة الإدارة، وهذا **بالضبط ما تريده**.

لكن هناك تسريب حقيقي، والتقرير مرّ عليه:

`backend/app/modules/catalog/schemas.py:76` — `ProductCard`، وهو المخطط الذي يراه كل زائر:

```python
class ProductCard(BaseModel):
    ...
    price_max: float | None = None      # ← سطر 85: نطاق السعر، وأنت تريد ثمناً واحداً
    lead_time_days: int | None = None   # ← سطر 94: وقت التصنيع، وأنت تريد حذفه
```

**الخلاصة:** التقرير طلب إخفاء حقل مخفيّ أصلاً، وترك الحقلين المكشوفين فعلاً.
الإصلاح الصحيح: احذف `price_max` و `lead_time_days` من `ProductCard`.

### ٢.٢ بند ناقص وخطير — رقم ٩ (`CustomRequest.product_id`)

التقرير قال «احذفه». الحذف صحيح **لكنه بمفرده يُحوّل عطباً شبه دائم إلى عطب دائم ١٠٠٪**.
التفصيل في §٣.١ أدناه — **لا تنفّذ البند ٩ قبل قراءته.**

### ٢.٣ بند أصعب بكثير مما وُصف — رقم ٨ (حذف `kind`)

التقرير وصفه بـ «`kind` + ٨ مواضع». العدّ الحقيقي: **٢٠ موضعاً**، ومنها ثلاثة لا تظهر في بحث نصي عادي:

- `backend/app/modules/feed/engine.py:141` — **SQL خام** (`p.kind = 'workshop'`)
- `backend/app/modules/admin/metrics.py:362` — مقارنة بنص (`Product.kind == "shelf"`)
- `backend/app/modules/catalog/schemas.py:225-237` — مُحقِّق `coherent_for_its_kind` يفرض قواعد كاملة

وهذه المواضع الثلاثة **هي نفسها أعطاب** (§٣.٣ و §٣.٥). لذلك الترتيب مهم:
أصلح الأعطاب أولاً، وعندها يصبح حذف `kind` عملاً ميكانيكياً بلا مخاطر.

### ٢.٤ تحذير على البند ٢٠ (`ready`)

`ready` ليست حالة معلّقة — هي مذكورة في `ALLOWED_TRANSITIONS` (`models/orders.py:43-44`)
وفي نصوص الإيميل (`notify/service.py:68`). دمجها = migration + تعديل نصوص + تعديل آلة الحالات.
قابلة للتنفيذ، لكنها ليست «مراجعة» كما وُصفت.

---

## ٣. ما لم يره التقرير — ٦ مشاكل، اثنتان منها توقفان العمل

### ٣.١ 🔴 كل طلب خاص يُنشئ فاتورة فارغة — بلا أي سطر

`backend/app/modules/requests/service.py:125-136`:

```python
items=[
    OrderItem(product_id=request.product_id, ...)
]
if request.product_id      # ← الشرط
else [],                   # ← وإلا: لا شيء
```

و `backend/app/models/orders.py:123` يفرض:

```python
product_id: Mapped[str] = mapped_column(..., nullable=False, ...)  # FK RESTRICT
```

**السلسلة المنطقية:**

1. قاعدتك المؤكدة (§1.1 من التقرير): «الطلب الخاص **لا يبدأ من منتج في المتجر**»
2. إذن `request.product_id` يساوي `None` في كل طلب خاص حقيقي
3. إذن الشرط يفشل، والطلب يُنشأ بـ `items = []`
4. ولا يمكن إصلاحه بحذف الشرط، لأن `OrderItem.product_id` **لا يقبل `NULL`**

**النتيجة:** طلب في قاعدة البيانات قيمته `subtotal = 260`, `total = 260`، **وصفر أسطر**.
لوحة الإدارة تفتحه فارغاً. الفاتورة — حين تُبنى (البند ١٢) — ستُطبع فارغة.
والزبون يرى طلباً بلا محتوى.

**لماذا لم يُكتشف؟** `backend/tests/test_requests.py:110`:

```python
assert order.json()["subtotal"] == 260.0
assert order.json()["delivery_fee"] == 0.0
assert order.json()["total"] == 260.0
# ← لا سطر واحد يفحص order["items"]
```

الاختبار يفحص المبلغ ولا يفحص الشيء المُباع. والمساعد `ask()` (سطر ٢٢) لا يمرّر `product_id`
أبداً — أي أن **كل اختبار في الملف يمرّ عبر المسار المكسور ويمرّ بنجاح**.

**الحل:** اجعل `OrderItem.product_id` يقبل `NULL` (+ migration)، واجعل `approve()` يبني السطر
دائماً من الوصف والسعر المتّفق عليه. عندها يصبح حذف `product_id` (البند ٩) آمناً.

---

### ٣.٢ 🔴 الطلبات تُلغى تلقائياً بعد ٤٨ ساعة

`backend/app/modules/orders/service.py:64-94`:

```python
async def release_stale_reservations(db) -> int:
    cutoff = datetime.now(UTC) - timedelta(hours=RESERVATION_HOURS)   # 48
    stale = ... select(Order).where(
        Order.status == OrderStatus.placed,
        Order.created_at < cutoff,
    ).limit(50)
    for order in stale:
        await change_status(db, order, OrderStatus.cancelled, ...)
```

الدالة **لا تفحص القطع إطلاقاً** رغم اسمها ورغم تعليقها الذي يقول
«It is a no-op once they are done». ليست no-op: هي تُلغي **كل** طلب بقي في `placed`
أكثر من ٤٨ ساعة، بلا شرط.

وتعمل عند **كل** عملية checkout (`service.py:136`) — أي أن زبوناً جديداً يشتري
فيتسبب في إلغاء طلبات زبائن آخرين. و `change_status` يُرسل إيميلاً
(`notify/service.py:97`): «Your order is cancelled. You owe nothing.»

**لماذا هذا خطير عندك تحديداً:** مرحلة `placed` عندك تنتهي **بمكالمة هاتفية منك**.
عطلة، سفر، انشغال يومين ← كل الطلبات المعلّقة تُلغى، والزبائن يتلقّون إيميل إلغاء.

وهذا يناقض قاعدتك المؤكدة نصاً: «المخزون: لا يوجد إطلاقاً. **الطلب لا يُرفض أبداً**».

**الحل:** احذف الدالة ونداءها. لم يعد لها ما تُحرّره بعد أن توقف الحجز.

---

### ٣.٣ 🟠 الـ feed مكسور — ومفصول عن الموقع أصلاً

`backend/app/modules/feed/engine.py:139-145`:

```sql
WHERE p.status = 'active'
  AND ( p.kind = 'workshop'
        OR EXISTS (SELECT 1 FROM pieces pc
                   WHERE pc.product_id = p.id AND pc.state = 'available') )
```

جلسة `000cc10` («Stop counting stock») أزالت عدّ المخزون من `catalog/service.py:62-66`
لكنها **لم تُزله من هنا**. و `Piece` لا تُنشأ إلا يدوياً من
`POST /admin/products/{id}/pieces` (`catalog/routes.py:671` — الموضع الوحيد في المشروع كله).

**النتيجة:** أي منتج `shelf` لم تُدخل قطعه يدوياً → **محجوب من الـ feed**.

والأهم: **الـ feed غير مستعمل أصلاً.** `api.feed` معرَّف في `frontend/src/lib/api.ts:257`
ولا يناديه أحد. الصفحة الرئيسية تنادي `/products` (`page.tsx:53`)، وهو مسار **سليم**
لا يحمل هذا الشرط. القارئ الوحيد للـ feed هو معاينة الإدارة
(`admin/manage/feed/page.tsx:60`) — التي ستُظهر لك قائمة شبه فارغة وتبدو معطوبة.

**هذا قرار لك، لا إصلاح:** محرّك الـ feed كان جوهر خطة إعادة البناء (المرحلة ٢)،
وهو الآن كود يتيم. إمّا أن يُوصَل بالواجهة (ويُصلَّح الشرط)، أو يُحذف كما حُذف غيره.
أضفه إلى أسئلة §5 في التقرير السابق.

---

### ٣.٤ 🟠 لوحة الإدارة تقول لك إن المتجر فارغ

`backend/app/modules/admin/metrics.py:356-372` يَعدّ صفوف `Piece` لكل منتج `shelf` نشط،
ثم:

```python
if available == 0:
    entry["note"] = "nothing left"
    running_out.append(entry)
```

بما أن القطع لم تعد تُنشأ، `available` يساوي صفراً دائماً ← **كل منتج على الرف
يُدرَج تحت «نفد»**. لوحة تحكّمك تعرض عليك متجراً خاوياً بينما كل شيء متاح.

**الحل:** احذف اللوحة. هي بُنيت لتقيس مخزوناً قرّرتَ ألّا تتبعه.

---

### ٣.٥ 🟡 الطلب الخاص بلا تاريخ تسليم على صفحة التتبّع

`requests/service.py:111-138` يبني `Order` بدون `promised_for`، رغم أن
`request.promised_for` حُسب عند التسعير (`service.py:79`).

`Order.promised_for` هو ما يقرأه العدّاد على صفحة الزبون (`models/orders.py:97`).
النتيجة: مسار المتجر فيه عدّاد، ومسار الطلب الخاص بلا عدّاد — رغم أن الطلب الخاص
هو المسار الذي يحتاج الطمأنة أكثر.

**الحل:** سطر واحد — `promised_for=request.promised_for` في بناء الـ `Order`.

---

### ٣.٦ 🟡 زر «موافق» يُنشئ سجلاً مالياً بلا أي مصادقة

`backend/app/modules/requests/routes.py:137` — `POST /requests/track/{token}/approve`
لا يطلب حساباً ولا تحققاً؛ الرمز وحده يكفي، وهو يُنشئ `Order` بمبلغ.

هذا يتقاطع مع البند ١٩ في التقرير السابق (أنت تريد التأكيد **بالهاتف**).
حين تُنفّذ البند ١٩ فالمسار الصحيح ليس تعطيل الزر في الواجهة فقط —
بل **حذف الـ endpoint**، ونقل الحالة يدوياً من لوحتك بعد المكالمة.
تعطيل زر في الواجهة لا يُغلق مساراً في الـ API.

---

## ٤. الدرس الذي يفسّر كل ما سبق

الأعطاب الستة أعلاه ليست ستّ أخطاء مستقلة. كلها من **عائلة واحدة**:

> جلسة قرّرت حذف المخزون. حذفته من الموضع الذي كانت تعمل فيه، وتركت أثره في
> أربعة مواضع أخرى: `feed/engine.py`، `metrics.py`، `orders/service.py`،
> و `models/orders.py`. لا فرع `main` يجمعها، ولا اختبار يفحص الشيء المُباع.

ولهذا فترتيب `RAPPORT.md` يحتاج تعديلاً: هو يبدأ بـ «الأسهل أولاً»، وهذا صحيح
لولا أن مشكلتين من الفئة الحمراء تعملان في الإنتاج الآن.

---

## ٥. خطة التنفيذ المصحّحة

| المرحلة | المحتوى | لماذا هنا |
|---|---|---|
| **٠ — GitHub** | ضبط `main` افتراضياً + حذف الفروع الثلاثة | §1 — **بانتظارك، لا أملك صلاحيته** |
| **١ — إيقاف النزيف** | ✅ **مُنفَّذة** — §3.2 (الإلغاء التلقائي) · §3.5 (تاريخ التسليم) · البند ٢١ (`python-jose`) · البنود ٢٢-٢٤ (رؤوس nginx) | راجع §8 |
| **٢ — إصلاح الطلب الخاص** | §3.1 (`OrderItem.product_id` يقبل NULL + بناء السطر) ثم البند ٩ | البند ٩ **بعد** §3.1، لا قبله |
| **٣ — دفن المخزون نهائياً** | §3.3 · §3.4 · البنود ١-٤ | كلها نفس العائلة، تُنفَّذ دفعة واحدة |
| **٤ — مواءمة الحقول** | البنود ٥-٨، ١٠، و §2.1 (`price_max` + `lead_time_days` من `ProductCard`) | migration واحد بعد أن تنظّف المرحلة ٣ نصف المواضع |
| **٥ — المراحل والحالات** | البنود ١٧-٢٠ · §3.6 (حذف الـ endpoint لا الزر) | |
| **٦ — الفواتير** | البنود ١٢-١٥ | **بعد** المرحلة ٢ حصراً — فاتورة فوق طلب فارغ لا معنى لها |
| **٧ — الإلغاء** | البند ١٦ | |
| ~~**٨ — الأمن**~~ | ✅ نُفِّذت مع المرحلة ١ | |

**تغييران جوهريان عن ترتيب `RAPPORT.md`:**

- الفواتير (كانت المرحلة ٤) **لا يمكن** أن تسبق إصلاح §3.1 — كنت ستبني مولّد PDF
  فوق طلبات بلا أسطر، وتكتشف ذلك بعد انتهاء العمل.
- الإلغاء التلقائي (§3.2) يُلغي طلبات زبائنك الآن، وهو إصلاح بحذف ثلاثين سطراً.

---

## ٦. قاعدة تمنع تكرار هذا

كل الأعطاب الحمراء مرّت لأن الاختبارات تفحص **المبالغ** ولا تفحص **الأشياء**.

اطلب في كل جلسة قادمة:

> أي اختبار يفحص طلباً يجب أن يفحص `items` — عددها، وعناوينها، وأسعار وحداتها —
> لا مجاميعها فقط.

هذا السطر وحده كان سيكشف §3.1 يوم كُتب.

---

## ٧. ما بقي معلّقاً

أسئلة §5 من `RAPPORT.md` الستّة ما زالت بلا جواب، ويُضاف إليها:

7. **محرّك الـ feed** — يُوصَل بالواجهة أم يُحذف؟ (§3.3)
8. **جدول `Piece`** — هل ستُدخل القطع يدوياً يوماً، أم يُحذف الجدول نهائياً؟
   الإجابة تحسم البنود ١-٤ و §3.3 و §3.4 دفعة واحدة.

---

---

## ٨. ما نُفِّذ فعلاً (المرحلة ١)

أربعة إصلاحات، ٣٠ سطراً مضافاً و٥٢ محذوفاً، بلا أي migration:

| # | الملف | التغيير |
|---|---|---|
| §3.2 | `backend/app/modules/orders/service.py` | حُذفت `release_stale_reservations` و `RESERVATION_HOURS` ونداؤها من `place_order`. **لم يعد أي طلب يُلغى تلقائياً.** حُدِّث وصف الوحدة الذي كان يصف حجزاً لم يعد موجوداً |
| §3.5 | `backend/app/modules/requests/service.py` | `promised_for=request.promised_for` — عدّاد التسليم صار يظهر للطلب الخاص |
| ٢١ | `backend/requirements.txt` | `python-jose` ‏`3.3.0 → 3.5.0` (تحققتُ: الإصدار موجود على PyPI، والاستعمال عندك `jwt.encode/decode/JWTError` فقط — ترقية بلا تغيير كود) |
| ٢٢-٢٤ | `infra/nginx/conf.d/mostyle.conf` | `X-Frame-Options` + `Referrer-Policy` + `X-Content-Type-Options` على مستوى `server` |

**نقطة تستحق الانتباه في nginx:** `add_header` **لا يُورَّث** إلى أي `location` يُعلن رؤوسه الخاصة.
`location /_next/static/` كان يُعلن `Cache-Control` — أي أنه كان **يُسقط `Strict-Transport-Security`
الموجود أصلاً** قبل تغييري. كرّرتُ المجموعة كاملة داخله، فأُصلح تسريب قديم مع الجديد.

### ما تحقّقتُ منه وما لم أتحقق منه

| ✅ تحقّقت | ❌ لم أستطع |
|---|---|
| `python -m py_compile` على الملفين | **تشغيل الاختبارات** — تحتاج Postgres + pgvector، غير متاحين هنا |
| `ruff check` — نظيف | **`nginx -t`** — nginx غير مثبّت. فحصتُ توازن الأقواس يدوياً (١٥/١٥) |
| لا مرجع متبقٍّ لـ `release_stale_reservations` في المشروع كله | |
| لا اختبار كان يستدعيها (فحصتُ قبل الحذف) | |

**شغّل الاختبارات قبل الدمج.** التغييرات كلها حذف أو سطر واحد، لكن لم يمرّ عليها مُشغِّل.

### ما لم ألمسه عمداً

المشكلة الحمراء الأولى (§3.1 — الطلبات الفارغة) تحتاج **migration** على `OrderItem.product_id`،
وقرار تصميم في بناء سطر الطلب. لا أُنفّذ migration على قاعدة بياناتك دون أن تراه أولاً — وهو أول
ما نبدأ به حين تستيقظ.

---

*كل موضع في هذا الملف مكتوب بصيغة `ملف:سطر` — قابل للنقر في VS Code.*
