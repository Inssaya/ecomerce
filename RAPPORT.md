# تقرير: قواعد العمل، المشاكل، والحلول المقترحة

> أُعدّ هذا التقرير بعد جلسة أسئلة مع المالك ومراجعة الكود على الفرع `main`.
> **لم يُنفَّذ أي تغيير في الكود** — ما عدا إنشاء فرع `main` (مذكور في §2).

---

## 1. قواعد العمل — ما تريده أنت

### 1.1 المبدأ الأساسي

الموقع فيه **مساران منفصلان تماماً**، يبدوان متشابهين لكنهما ليسا كذلك:

| | **المتجر** | **الطلب الخاص** |
|---|---|---|
| نقطة البداية | منتج معروض نضعه نحن | فكرة الزبون هو |
| الثمن | **معروف مسبقاً** | **غير معروف** — يُحدَّد بعد المعاينة |
| دور المكالمة | تأكيد فقط | جمع معلومات + تحديد الثمن |

الطلب الخاص **لا يبدأ من منتج في المتجر**. يبدأ من رأس الزبون، شرط أن يكون داخل نطاق فئاتنا (مثال: نظارات مطبوعة ثلاثية الأبعاد).

### 1.2 مسار المتجر

```
١. الزبون يرسل الطلب          → placed
٢. نحن نراه                   (عمل داخلي، لا حالة)
٣. نتصل به للتأكيد            → confirmed
٤. الإعداد والتغليف           → preparing
٥. الإرسال                    → out_for_delivery
٦. الاستلام                   → delivered
```

### 1.3 مسار الطلب الخاص

```
١. الزبون يشرح طلبه + صورة/ملف
٢. نتواصل معه لمزيد من المعلومات
٣. نرسل فاتورة + نتصل لتأكيد الطلب والثمن
٤. الإعداد
٥. الإرسال
٦. الاستلام
```

### 1.4 القواعد المؤكدة

| القاعدة | التفصيل |
|---|---|
| **المخزون** | لا يوجد إطلاقاً — لا ظاهر ولا داخلي. الطلب لا يُرفض أبداً |
| **وقت التصنيع** | يُحذف نهائياً من كل مكان |
| **وقت التوصيل** | يبقى في قاعدة البيانات — **لوحة الإدارة فقط**، مخفي عن الزبون. سيُعرض مستقبلاً عند توسّع العمل |
| **السعر** | منتوج واحد = ثمن واحد. لا `price_min` ولا `price_max` ولا نطاق |
| **التخفيض** | اختياري لكل منتج، بالنسبة **أو** بالرقم، يُشغَّل ويُطفأ بحرية |
| **الاسم الشخصي** | اختياري لكل منتج، لا يغيّر شيئاً في المسار — نكتب اسمه على طلبه فقط |
| **الفاتورة (المتجر)** | PDF، تظهر في صفحة تتبّع الطلب + تُرسل تلقائياً بالإيميل بعد الـ checkout |
| **الفاتورة (الخاص)** | تُرسل بعد تحديد الثمن النهائي، **قابلة للتعديل من طرفنا دائماً** |
| **محتوى الفاتورة** | الاسم، العنوان، كل معلومات الزبون، تفاصيل الطلب، التاريخ |
| **الإلغاء** | الزبون **المسجّل** فقط، وفقط في مرحلة `placed`. بعد مكالمة التأكيد → يتواصل معنا في الخاص |
| **مشاكل التنفيذ** | نتواصل مع الزبون شخصياً — **لا يُذكر هذا إطلاقاً في الواجهة الأمامية** |

---

## 2. مشكلة البنية — السبب الجذري (✅ أُصلحت)

**المشكلة:** المستودع لم يكن فيه فرع `main` ولا فرع افتراضي. ثلاثة فروع `claude/*` فقط، وكل جلسة تتفرّع من حيث صادفت.

**النتائج المباشرة لهذا:**
- جلسة حذفت `kind` ثم تراجعت عنه كتابةً (`000cc10`)
- جلسة أضافت وقت توصيل لكل منتج بينما المالك لا يريده ظاهراً
- المراجعة السابقة (`CODE_REVIEW.md`) كُتبت ضد كود ميت بـ 39 commit — وكانت مضلِّلة

**ما فُعل:** أُنشئ فرع `main` من `claude/monolith-rebuild-5xzsfl` (الأحدث، يحتوي الجلسات الأربع) ورُفع إلى GitHub. أُعيد بناء فرع العمل منه.

**ما يجب فعله في VS Code:**
1. اضبط `main` كفرع افتراضي في إعدادات GitHub (Settings → Branches → Default branch)
2. احذف الفرعين القديمين بعد التأكد: `claude/ecommerce-saas-microservices-5xzsfl` (متطابق مع سلف `main`) و `claude/codebase-line-by-line-review-9szy8p`
3. أي جلسة قادمة يجب أن تتفرّع من `main` حصراً

---

## 3. المشاكل المكتشفة والحلول المقترحة

### 3.1 كود ميت يجب حذفه

| # | المشكلة | الموقع | الحل |
|---|---|---|---|
| 1 | جدول `pieces` وكل حالاته (`available/reserved/sold/kept`) — نظام الحجز حُذف لكن الجداول بقيت | `backend/app/models/catalog.py`, `models/__init__.py` | حذف `Piece` و `PieceState` + migration |
| 2 | `PIECE_STATE_FOR_ORDER` يشير إلى `PieceState` رغم أن لا شيء يحجز | `backend/app/modules/orders/service.py:54` | حذف الخريطة كاملة |
| 3 | تعليق «يُعيد القطع إلى الرف» لسلوك غير موجود | `backend/app/modules/orders/service.py:257` | حذف |
| 4 | `show_piece_numbers` وأرقام القطع (`01/03`) | `backend/app/models/catalog.py` | حذف |

### 3.2 حقول تخالف قواعد العمل

| # | المشكلة | الموقع | الحل |
|---|---|---|---|
| 5 | `lead_time_days` — وقت التصنيع، والمالك لا يريده | `models/catalog.py:233`, `models/orders.py:147`, `models/requests.py:109`, `models/local.py:87` | حذف من الأربعة |
| 6 | `CheckConstraint` يفرض `lead_time_days` على كل منتج `workshop` | `models/catalog.py:167` | حذف القيد + migration |
| 7 | `price_max` — نطاق سعر، والمالك يريد ثمناً واحداً | `models/catalog.py:235` | حذف |
| 8 | `kind` (shelf/workshop) — بعد حذف المخزون ووقت التصنيع و `price_max` لم يعد يميّز أي شيء | `models/catalog.py` + 8 مواضع استعمال | حذف `ProductKind` وكل فروعه |
| 9 | `CustomRequest.product_id` — بُني لحالة «أريد هذا المنتج معدّلاً»، والمالك يقول الطلب الخاص يبدأ من فكرة الزبون لا من منتج | `models/requests.py:81` | حذف |
| 10 | `Quote.lead_time_days` **إجباري** — سينكسر عند حذف وقت التصنيع | `modules/requests/schemas.py:50` | استبداله بتاريخ تسليم أو حذفه |
| 11 | `delivery_days` يظهر للزبون | `modules/catalog/schemas.py` | إبقاء الحقل، **إزالته من مخططات الاستجابة العامة** فقط |

### 3.3 ميزات ناقصة تماماً (تحتاج بناء)

| # | ما ينقص | الحالة الحالية |
|---|---|---|
| 12 | **فاتورة PDF** | لا توجد أي مكتبة PDF في `backend/requirements.txt`. الفاتورة الوحيدة هي عرض للطباعة في لوحة الإدارة (`frontend/src/app/admin/orders/OrderPopup.tsx`) |
| 13 | **الزبون يرى فاتورته** | صفحة التتبّع `frontend/src/app/[lang]/track/[token]/page.tsx` موجودة لكن بلا فاتورة |
| 14 | **إيميل فاتورة تلقائي بعد الـ checkout** | يوجد إيميل تغيّر حالة فقط (`modules/notify/service.py:261`)، لا فاتورة |
| 15 | **فاتورة للطلب الخاص + تعديلها** | غير موجودة إطلاقاً |
| 16 | **إلغاء الطلب من طرف الزبون** | **لا يوجد أي endpoint للإلغاء**. حالة `cancelled` موجودة لكن المالك وحده يضعها من `/admin/orders/{reference}/status` |
| 17 | **مرحلة «نتواصل لمزيد من المعلومات»** في الطلب الخاص | ينتقل من `requested` إلى `quoted` مباشرة |
| 18 | **مرحلة الشحن** في الطلب الخاص | ينتقل من `ready` إلى `delivered` مباشرة |

### 3.4 سلوك يخالف ما تريده

| # | المشكلة | الموقع | الحل |
|---|---|---|---|
| 19 | `approved` في الطلب الخاص = **زر يضغطه الزبون**، والمالك يريد التأكيد **بالهاتف** | `modules/requests/routes.py:71` (`/requests/track/{token}/approve`) | حذف الزر من الواجهة؛ المالك ينقل الحالة يدوياً بعد المكالمة |
| 20 | مرحلة `ready` في الطلب زائدة عن الست مراحل التي وصفها المالك | `models/orders.py` (`OrderStatus`) | مراجعة: دمجها مع `preparing` أو `out_for_delivery` |

### 3.5 أمن

| # | المشكلة | الحل |
|---|---|---|
| 21 | `python-jose[cryptography]==3.3.0` — **CVE-2024-33663 و CVE-2024-33664** | ترقية إلى `3.5.0` في `backend/requirements.txt` (لا تغيير في الكود) |
| 22 | `X-Frame-Options` غير موجود في nginx | إضافته على مستوى `server` في `infra/nginx/conf.d/mostyle.conf` |
| 23 | `Referrer-Policy` غير موجود | نفس الملف |
| 24 | `X-Content-Type-Options` موجود في `location` واحد فقط، لا على مستوى `server` | نقله إلى مستوى `server` — انتبه: `add_header` **لا يُورَّث** في أي `location` يعلن رؤوسه الخاصة |

> ملاحظة: بقية الإصلاحات الأمنية من المراجعة السابقة (رفض `JWT_SECRET` الفارغ، منع الدورة في شجرة الفئات، تهريب قيم الإيميل) **موجودة فعلاً** على `main` — أصلحتها الجلسات الأربع بشكل مستقل.

---

## 4. ما هو مبنيّ بشكل صحيح — لا تلمسه

| الميزة | الموقع | لماذا هو صحيح |
|---|---|---|
| نظام التخفيض | `models/catalog.py:204-208` | `discount_kind` (نسبة/رقم) + `discount_value` + `discount_active` — يطابق طلبك حرفياً، ويُطفأ دون فقدان القيمة |
| حساب السعر | `models/catalog.py:274` (`effective_price`) | مصدر واحد للحقيقة — الإدارة والصفحة والسلة وسطر الطلب كلها تقرأ منه، فلا تتناقض |
| الحماية من السعر السالب | `models/catalog.py:290` | تخفيض 120% أو 200 درهم على منتج بـ150 لا يجعله مجانياً |
| `personalizable` | `models/catalog.py:217-218` | `Boolean` لكل منتج + نسبة زيادة — اختياري بالضبط كما تريد |
| احتساب الزيادة في الخادم | جلسة 2 | زبون يكذب في الطلب يُحاسَب بالسعر الصحيح |
| آلة حالات الطلب | `models/orders.py` | انتقالات محدّدة — «أي حالة من أي حالة» كانت خطأً حقيقياً في الكود القديم |
| صفحة التتبّع + البحث بالمرجع/الهاتف | `[lang]/track/[token]`, `POST /orders/find` | موجودة وتعمل |

---

## 5. أسئلة لم تُحسم بعد

1. **طرق الدفع** — الدفع عند الاستلام فقط؟ أم هناك غيره؟
2. **رسوم التوصيل** — يوجد `delivery_fee` على الطلب و `delivery_days_min/max` لكل مدينة في `models/local.py`. ما القاعدة بالضبط؟
3. **عمق الفئات** — فئة ← فئة فرعية فقط، أم أعمق؟
4. **المساعد الذكي** (`modules/ai/shopper.py`) — تريده أم يُحذف؟ يستهلك جزءاً كبيراً من الكود
5. **الخصائص بدل الـ variants** — جلسة 1 استبدلت `variants` بجدول `attributes` (مقاسات، ألوان، مواد). هل هذا ما تريده؟
6. **مرحلة `ready`** في الطلب — تبقى أم تُدمج؟

---

## 6. ترتيب التنفيذ المقترح

**المرحلة 0 — قبل أي كود**
- ضبط `main` كفرع افتراضي على GitHub
- حذف الفروع القديمة

**المرحلة 1 — التنظيف (خطر منخفض، يُصغّر الكود)**
- المشاكل 1-4: حذف `pieces` وكل آثاره
- المشكلة 21: ترقية `python-jose` (سطر واحد)
- المشاكل 22-24: رؤوس nginx

**المرحلة 2 — مواءمة قواعد العمل (يحتاج migration)**
- المشاكل 5-11: حذف `lead_time_days`, `price_max`, `kind`, `CustomRequest.product_id`
- إخفاء `delivery_days` عن الزبون

**المرحلة 3 — المراحل والحالات**
- المشاكل 17-20: مراحل الطلب الخاص، وتحويل `approved` إلى تأكيد هاتفي

**المرحلة 4 — الفواتير (الأكبر)**
- المشاكل 12-15: مكتبة PDF، فاتورة في صفحة التتبّع، إيميل تلقائي، فاتورة الطلب الخاص القابلة للتعديل

**المرحلة 5 — الإلغاء**
- المشكلة 16: endpoint إلغاء للزبون المسجّل، في مرحلة `placed` حصراً

---

*كل رقم مشكلة في هذا التقرير قابل للإحالة — استعمله في VS Code للانتقال إلى الملف والسطر مباشرة.*
